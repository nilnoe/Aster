# Audits — 架构审计登记表

WORKFLOW 第 8 步（Architecture Audit）的强制产物。每个切片在审计完成后追加一行；
审计必须留痕，**无记录的审计视为未执行**（宪法 Rule 13 精神；ADR-022 决策 3）。

| 切片 / 事项 | Commit | 审计项 | 结论 | 违规与处置 | 日期 |
| --- | --- | --- | --- | --- | --- |
| BUG-018 关闭卡死修复（over-release 停笔 + 孤儿全局决策 + 看门狗） | a513639 | 行为证据：① 机制 ①——崩溃报告（swift-backtrace 2026-08-03 14:15，用户补充菊花）主线程栈 objc_release → _Block_release → NSConcretePointerArray dealloc → CA::Context::commit_transaction = in-flight drawable 双重释放；修复 = beginClosing（windowShouldClose 放行时停定时器 + isClosing，draw 关闭态返回），2 项停笔回归（放行停笔 / 取消保活）先红后绿；② 机制 ②——孤儿未决 × 关最后窗口 × 取消 = 终止重触发循环，2 项回归先红后绿（取消保窗只决策一次 / 保存全部关窗前固化孤儿终止零弹窗）；③ 看门狗冒烟（block 弱引用无保留环，start/stop 幂等）；④ 门禁——App 116 + Bridge 20 + Core 148 全绿，swift-format / cargo fmt+clippy 零告警，变异门禁 5/5；Rule 3（DocumentLifecycleIntegrationTests 346 → 197 + AppWindowCloseFlowTests 154 拆分）；Rule 9 三问（1 个 isClosing 状态 + 1 个 beginClosing + 1 个诊断类型，0 抽象层 / 0 依赖）；Rule 17（关闭决策与渲染关闭态各自单一入口，未新增状态机）；Rule 20（无保存域语义改动） | Pass | 无 | 2026-08-03 |
| 宪法 V1.5 治理铁律（Rule 17~21） | 503c02d | 行为证据：纯文档切片——constitution Version 1.4 → 1.5（新增 Rule 17 域状态单一所有者 / 18 不变量由方法保证 / 19 功能先冲突扫描 / 20 语义冻结 / 21 公共面总账机械化），changelog / roadmap / experience 同步；Rule 9 三问（0 代码 / 0 抽象层 / 0 依赖）；Rule 15（规则文本与 WORKFLOW 冲突扫描、ADR-023 v1.7、ADR-024 交叉一致；ADR 索引无漂移）；修订经项目所有者确认（用户「直接落实」）；Rule 19 自举验证（本切片 = 纯文档，触碰子系统 = docs 体系，无代码回归） | Pass | 无 | 2026-08-03 |
| T-070 文档生命周期状态收拢（ADR-025 / ADR-024） | 9d2cccf | 行为证据：① 收拢——AppDelegate 七个账本字段删除，Session 单一入口（FFI 21 项取代旧 16 项；ffi-ledger.py 实测 50 项）；② 不变量——Core session 集成测试 16 项（未决 ⟺ 缓冲行 / 保存失败保全 / 崩溃往返 / 磁盘基线）+ App SaveStateInvariantTests 6 种子 × 60 步经 Session 断言全绿；③ BUG-019~023 修复带回归（跨 frame 关闭隔离 / 关闭即注销 / 恢复后 ⌘S 成功 / 失败提示按文档 1→1→2）；④ 门禁——cargo fmt / clippy -D warnings / swift-format 零告警，Core 148 + Bridge 20 + App 111 全绿，变异门禁 5/5（M1 / M2 / M5 迁至 core/src/session.rs；mutate.py 增 Rust 变异后产物还原——实测 M5 残留产物导致全量测试连环失败，已修）；Rule 3（session.rs 299 / bridge_session.rs 161 / session_error.rs 44，全部 ≤300；core/src 2577 ≤20k，app/Sources 2326 ≤5k）；Rule 9 三问（1 模块 + 1 错误类型 + 21 FFI，删 16 旧 FFI + App 账本同步 ~150 行，净复杂度下降，0 抽象层 / 0 依赖）；Rule 14（新 FFI 全部 App 消费；旧 document_manager / store / snapshot 面撤销）；Rule 15（ADR-024 FFI 总账机械化 + CI 门禁） | Pass | 无 | 2026-08-03 |
| BUG-018 Frame 关闭卡死排查 + 闪烁保留环 | 7c1a76b | 行为证据：① 用户报告卡死——进程内穷尽复现（dispatch performClose / beginModalSession + performClose / NSApp.postEvent 真实鼠标事件到关闭按钮 / 真实 NSAlert runModal / MTKView 真实 GPU render + 循环定时器）全部正常关闭，挂起未复现（如实记录，状态 Open 待用户补充信息）；② 已确认缺陷：probe 实测 isReleasedWhenClosed=true 在 ARC 下不生效、关闭后窗口仍被 AppKit 保留 + Timer target 强持有形成保留环 → CaretBlinker 抽取 + windowWillClose 确定性 stop；回归 3 项（停止闪烁 / performClose 空文档 / 未决保存全部）先红后绿；App 113 + Bridge 20 + Core 131 全绿 + 变异门禁 5/5；Rule 3（MetalView 305 → 278 + CaretBlinker 48 拆分）；Rule 9 三问（1 个状态机类型 + 2 处接线，拆分依据 = Rule 3 超限 + 闪烁单一职责，与 MetalPipeline / VertexBuilder 同款先例 / 0 新依赖）；Rule 14（无新公共 API，模块内 internal） | Pass（已确认部分）；挂起未复现需用户补充 | 用户报告卡死未复现——按 bug-workflow「无法复现停下反馈」，已固定已确认缺陷并请求复现细节 | 2026-08-03 |
| T-069 新建 Frame（⌘⇧N） | f9547be | 行为证据：5 项新用例先红后绿（菜单 ⌘⇧N 绑定 + 修饰键 / 第二窗口独立文档与快照 / 跨 frame 编辑隔离（B 污染 A 断言）/ frame B 自动保存到自己的缓冲行 / 关闭移除登记）；存量 105 项迁移 currentFrame 后全绿（App 110）；变异门禁暴露重构盲区——M3（恢复漏写缓冲）在新链路下存活 → SaveStateInvariantTests 新增「恢复文档必须登记未决且有缓冲行」不变量（BUG-011 泛化），变异门禁 5/5 恢复；Rule 3（AppDelegate 343 → 266 + Frame 88 拆分）；Rule 9 三问（frames 数组 + 按 frame 接线，0 抽象层——NSWindow 直接承载，Rule 1 不为未来窗内分窗建类型 / 0 新依赖）；Rule 14（无新公共 API，App 模块 internal）；绑定解耦（菜单单一来源，keyDown 不处理快捷键，测试断言修饰键与 selector）；Cmd+Q / 关闭 / 恢复路径回归通过 | Pass | 无 | 2026-08-03 |
| BUG-017 关闭按钮路径死循环 / 卡死 | 0cfdc16 | 行为证据：独立 repro 复现（repro2：窗口关闭→取消→applicationShouldTerminate 连续重触发弹窗直到 watchdog；修复版 repro：windowShouldClose 拦截后取消保窗、只弹一次、终止不再重触发）；4 项回归先红后绿（保存放行+固化 / 丢弃放行 / 取消保窗+未决保留 / 无未决不弹提示）；App 105 + Bridge 20 + Core 131 全绿 + 变异门禁 5/5 捕获，swift-format / cargo fmt+clippy 零告警；Rule 3（AppDelegate 311 → 275 + CloseFlow 52 拆分）；Rule 9 三问（1 个共用决策函数 + windowShouldClose 接线，0 抽象层 / 0 新依赖）；Rule 14（无新公共 API，App 模块 internal）；Cmd+Q 行为不变（AppExitFlow 回归通过） | Pass | 无 | 2026-08-03 |
| T-067 未保存指示迁移系统原生 | 4906ade | 行为证据：契约测试先红后绿（旧实现编辑后 `isDocumentEdited` 为 false、标题「● Aster」→ 新实现 true / 标题纯「Aster」）；App 101 + Bridge 20 + Core 131 全绿，swift-format / cargo fmt+clippy 零告警；Rule 9 三问（标题赋值改 2 行 + 1 个系统属性，0 抽象层 / 0 新依赖）；Rule 11（系统能力 isDocumentEdited 替换自研前缀渲染，删除手拼逻辑）/ Rule 12（AppDelegate 284 ≤300，测试文件 200 ≤300）；Rule 14（无新公共 API）；Principle 4（不 fight 系统：关闭按钮红点为 macOS 平台约定） | Pass | 无 | 2026-08-03 |
| 收工沉淀（Phase 7 六切片完成，2026-08-03 会话记忆刷新） | 1e9ecd0 | 行为证据：纯文档切片——experience 现状速览计数实测更新（App 100 / Core 131 / Bridge 20；App 源码 2197 / Core 2182 行 wc 实测）、T-052~T-062 完成清单补 commit hash（4d7c81b / bbdcc8d / 07d15bc / a2ce6ab / 775ae75 / baadd05）、剩余候选风险收敛（T-053/055/057/060/061）；Rule 9 三问（0 代码 / 0 抽象层 / 0 依赖）；Rule 15（计数与 audits/roadmap 同步，防止 I-005 式漂移） | Pass | 无 | 2026-08-03 |
| T-062 变异测试工具化 | baadd05 | 行为证据：`python3 scripts/mutate.py` 实测 5/5 变异点全部注入→被预期捕获→自动恢复（M1 3.1s / M2 1.7s / M3 1.8s / M5 1.7s / M-charIndex 1.7s，退出码 0）；注入后工作树无残留（git status 仅新增脚本）；Rule 7（stdlib 子进程/文件/JSON，不引第三方）；Rule 9 三问（1 脚本 + 1 清单 = 85 + 55 行，0 抽象层 / 0 新依赖——脚本是流程自动化不是抽象层）；Rule 12（mutate.py 85 ≤300，scripts 不计 App/Core 预算）；Rule 14（无公共 API）；M6 挂起变体不可自动化已注明清单；变异点漂移保护（old 唯一匹配，否则跳过并计失败） | Pass | 无 | 2026-08-03 |
| T-056 存储损坏与迁移契约 | 775ae75 | 行为证据：Core 4 项（乱字节 / 截断头返回 Sqlite 错误不 panic；v0 schema 迁移到 v1 且可读写、迁移锚点推进；只读目录失败干净（euid 501 实测）；双连接同库已提交行跨连接可见 + 删除可见）+ App 1 项（损坏 buffer.sqlite 启动不崩、提示可见、保存提示准确）先跑全绿（负结果：未发现新缺陷，契约固化）；App 100 + Core 131 + Bridge 20 全绿，swift-format / cargo fmt+clippy 零告警；Rule 7/8（rusqlite 加入 dev-dependencies = 同版本已锁定依赖复用，非新增第三方）；Rule 9 三问（0 生产代码改动 / 0 抽象层）/ Rule 12（store.rs 测试 281、SaveFailurePathTests 154 ≤300）；Rule 14（无新公共 API） | Pass | 无 | 2026-08-03 |
| T-059 已知限制契约固化（BUG-016） | a2ce6ab | 行为证据：3 项契约测试先红后绿——打开第二文件未决保留（缓冲行 / 序号 / 退出保存全部覆盖）、恢复只呈现最新且其余可管、忽略后全部缓冲文档登记未决 + 序号 + 退出保存全部固化全部内容；BUG-016 用例旧实现报 42/43 未登记（Red），修复后 App 99 + Bridge 20 + Core 127 全绿；Rule 9 三问（忽略分支 1 个循环 + guard，0 抽象层 / 0 新依赖）/ Rule 12（AppDelegate+Storage 216、测试文件 ≤300）/ Rule 14（无新公共 API）；ADR-013 v1.4 规则 3/4 核对原文（不因忽略而失管 / 不留没被问过的文档）——实现偏离 ADR 由本切片修复，无需改 ADR（Implementation Bug） | Pass | 无 | 2026-08-03 |
| T-058 状态机随机序列扩展 | 07d15bc | 行为证据：操作空间 4→9 类 + 种子 3→6 + 步数 50→60，6 种子全绿（360 步不变量成立：未决必有快照序号 / 缓冲行⟺未决 / 快照序号唯一）；变异复验还原 BUG-011 修复后 seed 7 立即失败（恢复文档缺缓冲行、终局保存失败），确认扩展测试捕获历史缺陷；App 96 + Bridge 20 + Core 127 全绿，swift-format / cargo fmt+clippy 零告警；Rule 9 三问（0 生产代码改动 / 0 抽象层 / 0 依赖——纯测试扩展，LCG 复用既有）/ Rule 12（SaveStateInvariantTests 132 ≤300）；Rule 14（无新公共 API）；负结果如实记录（未发现新缺陷，保存状态机泛化成立） | Pass | 无 | 2026-08-03 |
| T-054 失败可见性审计（BUG-015） | bbdcc8d | 行为证据：目录只读注入写失败 → 自动保存失败按段落提示一次（saveErrorCount 0→1→1→2 序列验证防刷屏与成功复位）+ 未决标记保留；ASTER_STORE_DIR 指向普通文件 → 启动提示 + 保存提示准确（「存储未就绪」而非「没有可合并的快照」）；2 项用例先红后绿；App 93 + Bridge 20 + Core 127 全绿，swift-format / cargo fmt+clippy 零告警；Rule 9 三问（1 个布尔状态 + 3 处提示接线，0 抽象层 / 0 新依赖——复用既有 presentSaveError 接缝）/ Rule 12（AppDelegate 277、AppDelegate+Storage 210、SaveFailurePathTests 133 ≤300）/ Rule 14（无新公共 API，App 模块 internal）；ADR-023 v1.5 无模态弹窗原则：NSAlert 为过渡实现（与既有 presentSaveError 同档，T-026 统一移除） | Pass | 无 | 2026-08-03 |
| T-052 IME 契约审计（BUG-013 / BUG-014） | 4d7c81b | 行为证据：SDK `NSTextInputClient.h` 契约原文核对（characterIndexForPoint 的 point 为屏幕坐标系、返回字符索引；setMarkedText 必须替换 replacementRange）；模拟协议调用 5 项新用例先红后绿（EditorModel 2 + MetalView 3，含真实 NSWindow 屏幕坐标换算）；变异复验还原旧实现后 characterIndex 报 3≠1（字节 vs UTF-16）与 8≠1（屏幕点当视图点）；App 91 + Bridge 20 + Core 127 全绿，swift-format / cargo fmt+clippy 零告警；Rule 9 三问（2 处契约修复共 ~20 行，0 抽象层 / 0 新依赖）/ Rule 12（EditorModel 287、MetalView+Input 111 ≤300；测试文件 MetalViewTests 295 ≤300）/ Rule 14（无新公共 API，App 模块 internal）；踩坑：xctest 直接创建 NSWindow 挂载 MTKView 崩溃（SIGSEGV），需 orderFront 进入窗口服务器 | Pass | 无 | 2026-08-03 |
| 收工沉淀（2026-08-03 会话记忆） | d625df5 | 行为证据：纯文档切片——experience.md 现状速览更新（T-001~T-051 + BUG-010~012，App 86 / 127 / 20 全绿，行数 2139 / 2182 实测）、本会话完成清单（T-050 / BUG 修复 / T-051 / Phase 7 / ADR-006 v1.1）、决策速查表补充（独立快照序号 / committedText 基线 / 恢复写缓冲）、给下一个 agent 提醒（候选风险 / 测试方法论 / ADR-024 未决）；glossary 新增 4 术语（已固化文本 / 测试接缝 / 变异测试 / 状态机不变量）；Rule 9 三问（0 代码 / 0 抽象层 / 0 依赖） | Pass | 无 | 2026-08-03 |
| 数据结构评估落地（ADR-006 v1.1 + Phase 8 登记） | 34ca7e5 | 行为证据：纯文档切片（ADR-006 追加评估进展节 4 项热点/缺口 + roadmap Phase 8 T-063~T-066 + benchmarks 缺口记录 + experience 速查）；评估基于实现逐结构复核与 T-023 基线引用（layout_build_1mb 0.31ms / store_scratch_save_10k 10.66ms / bench 只测末尾已核实）；Rule 9 三问（0 代码 / 0 抽象层 / 0 依赖）；Rule 16（未做任何结构替换，只登记基准缺口与门禁）；Rule 13（T-065 标注反转 ADR-023 需用户确认） | Pass | 无 | 2026-08-03 |
| 测试专项 TODO 登记（Phase 7，T-052~T-062） | aed01f8 | 行为证据：纯文档切片（roadmap 新增 Phase 7 共 11 项测试计划 + changelog 记录 + T-034 状态漂移校正为 [x]）；Rule 9 三问（0 代码 / 0 抽象层 / 0 依赖，仅文档）/ Rule 15（roadmap 与 changelog 同步）；不执行测试（用户指示暂不覆盖） | Pass | T-034 状态漂移（完成于 440c75b 未更新）本切片校正 | 2026-08-03 |
| T-051 测试方法论强化 | b4d1406 | 行为证据：变异测试 6 变体实测（M1 全绿=盲区，M2~M5 被抓，M6 挂起暴露）；补测后 App 86 全绿（失败注入 2 + 不变量 3 种子×50 步）；M1 变异复验 4 失败确认盲区已补；Bridge 20 + Core 127 + 格式零告警；Rule 9 三问（0 生产代码改动 / 0 抽象层 / 0 依赖——纯测试方法论，LCG 为标准伪随机而非自研轮子，Rule 11）；Rule 12（测试文件 ≤300；SaveStateInvariantTests 86 行 / SaveFailurePathTests 80 行）/ Rule 14（无新公共 API）；踩坑：变异恢复需逐项 diff 核对（M1/M2 恢复补丁未对齐导致残留变异） | Pass | 无 | 2026-08-03 |
| BUG-010/011/012 修复（保存与恢复链路复审） | 435e3a0 | 行为证据：App 81 全绿（含 BugReproTests 3 个回归先红后绿：多文件保存互不覆盖 / 恢复后保存全部成功退出 / undo 回快照内容不置脏）+ Bridge 20 + Core 127 全绿；swift-format / cargo fmt+clippy 零告警；Rule 3（AppDelegate 274 / Storage 194 / BugReproTests 101 ≤300）/ Rule 9 三问（3 处保存状态修复 + 1 个比较基线字段，0 抽象层 / 0 新依赖，修复方案为最简：独立快照序号 / 写缓冲+登记 seq / 内容比较）/ Rule 12（App 源码 ≤5000）/ Rule 14（committedTextByDocId 为 App 模块内状态，非公共 API）；踩坑：History 相邻 Insert 合并规则再次干扰回归测试场景（改用选区替换拆步） | Pass | 无 | 2026-08-02 |
| T-050 App 集成测试套件（五组） | 520892b | 行为证据：App 78 全绿（新增 15 项：启动链路 3 + 恢复 3 + 文档生命周期 4 + 退出三分支 4 + 端到端 keyDown 1，真实 NSApplication / NSWindow / 落盘断言）；Bridge 20 + Core 127 + fuzz 3000 全绿；swift-format / cargo fmt+clippy 零告警；Rule 3（AppDelegate 262 / Storage 167 ≤300；测试文件 49~144 ≤300）/ Rule 9 三问（2 个提示 seam 方法 + 移除 final，0 抽象层 / 0 新依赖；测试基建是永久资产）/ Rule 12（App 源码 2100 ≤5000，测试不计预算）/ Rule 14（无新公共 API，seam 为 App 模块内 internal）/ 踩坑：跨模块覆写须类体方法、RustString 错误消息须 toString、SQLite 跨连接可见性、@MainActor XCTest setUp | Pass | 无 | 2026-08-02 |
| T-049 CI-Bench 稳定性（ADR-021 v1.3） | a55ba64 | 行为证据：本地实测 median + 200% 对比 exit 0；第二轮 CI 观测（+118.1% 误报、发布流水线同配置通过）数据驱动修订（Rule 16）；Rule 9 三问（脚本 1 函数 + 2 处阈值，0 抽象层 / 0 新依赖） | Pass | 无 | 2026-08-02 |
| T-048 发版 CI 检查修复 | 86d456a | 行为证据：本地实测 CI 同款 bench 命令 + 对比脚本（16 项 0 回归，exit 0）；CI-Rust 1.97 clippy 修复已在上一轮 CI 转绿；版本断言格式校验；Rule 9 三问（CI 配置 + 2 测试断言 + 1 脚本常量，0 抽象层 / 0 新依赖）；ADR-021 v1.2 修订留痕（阈值误报场景，Rule 16 数据驱动：实测噪声 +26%~+120%） | Pass | 无 | 2026-08-02 |
| T-047 空快照退出清理（ADR-023 v1.6） | 80adf3c | 行为证据：Core +2 + Bridge +1 + App 63 全绿（只删零长度 / 无关文件不动 / 目录缺失幂等）；ADR-023 v1.6 留痕（用户指示，Rule 13）；Rule 9 三问（1 方法 + 1 FFI + 1 处调用，0 抽象层 / 0 新依赖）/ Rule 12（snapshot.rs 227 ≤300；bridge_store.rs 89 ≤300） | Pass | 无 | 2026-08-02 |
| T-046 多文档状态全程检查（ADR-013 v1.4 / ADR-023 v1.5） | 5ac474c | 行为证据：App 63 全绿（PendingDocs 4 项 + 既有 59 项行为不变）；多文档退出流程（保存全部逐个合并 / 全部不保存 / 取消）；ADR-013 v1.4（缓冲 = 边界情况专用，干净退出后清空）+ ADR-023 v1.5（无模态弹窗原则，过渡实现标注）留痕（用户指示，Rule 13）；Rule 9 三问（1 个登记类型 + 合并循环，0 抽象层 / 0 新依赖）/ Rule 12（PendingDocs 40 行；AppDelegate 224 / 存储扩展 174 均 ≤300） | Pass | 无 | 2026-08-02 |
| T-045 缓冲生命周期（ADR-013 v1.3） | 8dacfff | 行为证据：Bridge 19 + App 59 全绿（delete 幂等 / 枚举消失 / 读回报错）；ADR-013 v1.3 生命周期规则（保留 4 类 / 删除 3 时机 / 不变量）；Rule 3（AppDelegate 334 → 209+145 拆分、bridge.rs 308 → 227+84 拆分，全部 ≤300）/ Rule 9 三问（1 FFI + 3 处接线 + 2 处拆分，0 抽象层 / 0 新依赖）/ Rule 12（delete_scratch 从死代码变真实消费） | Pass | AppDelegate / bridge.rs 超限由本切片自行拆分前置（同 T-018 先例） | 2026-08-02 |
| T-044 SQLite 保留论证（ADR-013 v1.2） | 7a1408d | 行为证据：纯文档切片（无代码改动）；论证基于代码现状核验（scratch/meta 在用、session 表零消费者、libsqlite3.a 2.4MB、FFI 6 项）；Rule 9 三问（0 抽象层 / 0 依赖 / 0 API，仅文档）；Rule 13（守则 b 把 session 表接线义务挂到 T-029，防悬挂）；决议保留经用户确认 | Pass | 无 | 2026-08-02 |
| T-043 崩溃恢复 v1（ADR-013 v1.1） | cab4045 | 行为证据：Core 2 + Bridge 1 + App 3 测试全绿（哨兵缺省/往返、缓冲枚举、恢复决策纯函数）；ADR-013 v1.1 修订留痕；Rule 9 三问（meta 表 1 行 + 3 方法 + 恢复提示，0 抽象层 / 0 新依赖）/ Rule 12（store.rs 288、AppDelegate 292 ≤300；core/src 2110、App 1945 在预算）/ Rule 14（3 FFI 消费者 = AppDelegate 恢复流程，同切片交付） | Pass | 无 | 2026-08-02 |
| T-042 快照改为纯文本（ADR-023 v1.4） | b8cd54b | 行为证据：Core 4（snapshot 模块全绿）+ Bridge 3 + App 全量 56 全绿；快照 .txt 纯文本断言（Buffer 可打开，用户反馈）；ADR-023 v1.4 修订留痕；Rule 3（snapshot 独立模块而非塞进 Store——Store=SQLite SRP）/ Rule 9 三问（0 抽象层 / 0 新依赖）/ Rule 12（snapshot.rs 172、store.rs 缩减；core/src 2039、App 1882 在预算）/ Rule 14（移除无消费者的 SQLite 快照 API） | Pass | 无 | 2026-08-02 |
| T-041 缓冲 + 快照保存模型（ADR-023 v1.3，BUG-009） | c4b4710 | 行为证据：Core 6 + Bridge 3 + App 1 测试全绿（快照序号 / 缓冲往返 / 合并读回 / 菜单 ⌘N / scratch id）；ADR-023 v1.3 + ADR-001 v1.2 修订留痕（用户指示，Rule 13）；BUG-009 根因 = onChange 只在 open() 接线，makeModel 统一接线修复；Rule 9 三问（三文件模型：缓冲连续写 + 快照提交，0 抽象层 / 0 新依赖）/ Rule 12（store.rs 267、AppDelegate 243 ≤300；core/src 1916、App 1880 在预算）/ Rule 14（open_scratch FFI 消费者 = newDocument，同切片交付） | Pass | 无 | 2026-08-02 |
| T-040 保存改为 SQLite 快照（ADR-023 v1.2） | d28cb9b | 行为证据：Core 6 + Bridge 2 + App 2 测试全绿（序号递增 / 缺号 / latest 往返 / 目录创建 / StorePaths）；ADR-023 v1.2 反转经用户确认（Rule 13：反转 Accepted 决策必须用户同意，changelog 记录）；Rule 4（open_next/open_latest + 1 错误变体 + FFI 4 项先 ADR）/ Rule 9 三问（0 抽象层 / 0 新依赖；civil date 标准算法，不引 chrono）/ Rule 12（store.rs 244、AppDelegate 171 ≤300；core/src 1872、App 1800 均在预算）/ Rule 14（撤销无消费者的 save_text / 错误变体） | Pass | T-037 方向偏差（磁盘写回超前实现）已由用户指示纠正，ADR v1.1 → v1.2 留痕 | 2026-08-02 |
| T-039 审计与门禁加固 | 3171806 | 行为证据：CI-Docs 新增审计完整性机械检查（未回填行 ≤1 + hash 全存在，fetch-depth 0，本地模拟通过）；CI-Release 补齐 swift-format(app/Sources) / fuzz / 规模预算 / docs 完整性 / 基准回归（I-008 对齐）；Rule 9 三问（纯 CI 配置与文档，0 依赖 / 0 抽象层）/ Rule 15（审计留痕从人工回填变为机械门禁） | Pass | 无 | 2026-08-02 |
| T-038 渲染数据路径重构 | 7220747 | 行为证据：3 个新测试 + 既有 49 项全绿（渲染 / 像素测试行为不变）；基准记录：每帧 O(n) → O(可见行)（全文 Bridge 往返与 split 只在失效后一次），帧时间全量基线随 T-016 建立；Rule 9 三问（缓存 2 字段 + 1 行切分 API + 二分，0 抽象层 / 0 依赖）/ Rule 12（EditorModel 249、VertexBuilder 202 ≤300；App 1775 < 5,000）/ Rule 3（EditorModel 单一职责未变） | Pass | 无 | 2026-08-02 |
| T-037 Disk 保存（ADR-023） | 135f44a | 行为证据：Core 5 + Bridge 2 + App 2 测试全绿（保存往返 / 失败可见 / 菜单 / onChange）；ADR-023 Accepted 且入索引（Rule 13 闭环）；Rule 4（1 方法 + 2 错误变体 + 1 FFI 先 ADR）/ Rule 9 三问（0 抽象层 / 0 依赖，App 薄胶水 ~96 行）/ Rule 12（AppDelegate 177、EditorModel 198、AppMenu 109 ≤300；App 总量 1711 < 5,000）/ Rule 14（onChange 是 App 模块内回调，非公共 API） | Pass | 无 | 2026-08-02 |
| T-035 Up/Down 边界修复（BUG-008） | 29bdf9d | 行为证据：4 个手写回归 + Up/Down 纳入属性差分 + 边界不变量显式断言（`PROPTEST_CASES=3000` 通过）；ADR-017 v1.1 备注（字节列须 floor 到边界，ADR-005 优先）；Rule 9 三问（1 行钳制 + 模型镜像算法，0 抽象层 / 0 依赖）/ Rule 12（property.rs 超 300 行 → 拆 support（119）/ property（162）/ property_fuzz（91），全部 ≤300；editor.rs 238 ≤300） | Pass | 无 | 2026-08-02 |
| T-034 审查问题登记 | 440c75b | Rule 15（issues.md 入 README 索引）/ Rule 3（issues.md 单一职责）/ Rule 9 三问（0 抽象层 / 0 依赖 / 0 公共 API，纯文档） | Pass（自审） | 无 | 2026-08-02 |
| 复审（全仓） | d996059、db053f5、bce9d05、fb82b85、aa78b6a | 文档漂移 / ADR 失约 / 零消费者模块 / 基准缺失 / 门禁缺口 | 发现并处置 | ADR-018 索引补登、宪法 V1.4（R13~16）、接线计划（T-015/016/024/028/029/031）、CI-Docs 与 CI-Release 门禁、T-023 基准落地 | 2026-08-02 |
| T-071 复审整改 II 问题登记（I-009~I-016） | 33ac830 | 行为证据：全仓复审实测——Core Theme / Command / Event 零生产消费者（`rg` 仅 bench/tests）；`Session::content_changed`（core/src/session.rs:104）只写 SQLite 不回写 DM 副本（双副本失鲜）；caret 公式三文件四处（MetalView.swift:245 / MetalView+Input.swift:78 / VertexBuilder.swift:114）；`layout_line_starts` 是 line_starts 唯一 Bridge 放行口；ffi-ledger.py 核验 FFI 面 50 项不变；Rule 9 三问（0 生产代码改动 / 0 抽象层 / 0 依赖，纯文档登记）；Rule 12（issues / roadmap 单一职责未变） | Pass | 无 | 2026-08-03 |
| T-072 行结构语义收拢（I-010，ADR-026） | 3901447 | 行为证据：Red = Bridge 编译失败（`layout_line_ranges` cannot find，2 项新测试）；Green = Bridge 22 + App 116 + Core 148 全绿（含 CJK 多行区间 [0,2]/[3,9]/[10,11] 断言与空文本 / 尾换行边界）；`python3 scripts/ffi-ledger.py` 输出 51 并同步 ADR-024；fmt / clippy / swift-format 零告警；Rule 9 三问（1 FFI + 机械分块助手，0 抽象层 / 0 依赖，查找保留标准二分理由 = 逐查询桥接拷贝 O(行数) > 本地 O(log n)）；Rule 14（消费者 = EditorModel.lineByteRanges 同切片交付）；Rule 12（EditorModel 287 行 ≤300，chunkRanges 单一职责） | Pass | 无 | 2026-08-03 |
| T-073 光标 / 组合几何收拢（I-011 / I-014 / I-016） | 44a2262 | 行为证据：Red = 编译失败（CaretGeometry / caretDisplayByte / contentVersion cannot find）；Green = App 120 全绿（116 → +4：CaretGeometry 2 / caretDisplayByte 1 / contentVersion 1；既有 BUG-004 / 006 / 007 滚动与 IME 回归不变）；swift-format 零告警；Rule 9 三问（1 个纯几何类型 + 2 个只读访问器 + 1 个测量缓存键，0 抽象层 / 0 依赖；宽度缓存 = 键值对而非抽象层）；Rule 14（CaretGeometry / caretDisplayByte / contentVersion 均 App 模块内，非公共 API）；Rule 12（CaretGeometry ~34 行、EditorModel ≤300，删除死代码 lines） | Pass | 无 | 2026-08-03 |
| T-074 frame 域收拢（I-012 / I-013） | 5a2dfb4 | 行为证据：Red = 编译失败（setFrameDocument / frameDocumentId / frameFileName cannot find）；Green = App 121 全绿（120 → +1 FrameDocument 登记契约；既有 FrameIntegration / AppWindowCloseFlow / DocumentLifecycle 全部经 frameDocs 断言通过）；swift-format 零告警；Rule 9 三问（1 个 struct + 3 个访问器，0 抽象层 / 0 依赖；显式参数替代实例字段 = 更简单方案）；Rule 14（FrameDocument 为 App 模块内类型，非公共 API）；Rule 12（FrameDocument ~25 行；AppDelegate 271 ≤300，删两套平行结构） | Pass | 无 | 2026-08-03 |
| T-075 架构决策（ADR-027 单 Buffer + Phase 8 集成排期） | 8ef62ed | 行为证据：全仓复核——双副本证据（AppDelegate.open 另建 Buffer / Session::content_changed 只写 SQLite 不回写注册表）；桥接消费面盘点（editor_text / session_content_changed / open_scratch 全部调用点）；ADR-017 text() → String 兼容性核对（bench_editor / property_fuzz / editor 测试均 String 与 &str 可比）；Rule 9 三问（1 共享句柄 + 1 工厂 + 2 签名变更，0 抽象层 / 0 依赖；RefCell = std，Rule 11）；冻结留痕（用户确认方向② + 性能第一） | Pass | 无 | 2026-08-03 |
| T-075 单 Buffer 实现（I-009，ADR-027） | ba5b9ee | 行为证据：Red = 新契约测试编译失败（session.editor / open_scratch(seed) / content_changed(id) cannot find）；Green = Core 150 全绿（+2：共享缓冲保鲜 / seed 注入）+ Bridge 24（+2：FFI 级同契约）+ App 121（生命周期 / 关闭流 / 崩溃恢复 / 状态机不变量全经新路径——行为零回归）；`python3 scripts/ffi-ledger.py` 输出 52 并同步 ADR-024；fmt / clippy / swift-format 零告警；Rule 9 三问（1 共享句柄 + 1 工厂 + 2 签名变更 + App 接线重写，0 抽象层 / 0 依赖；RefCell = std 内部可变性，Rule 11；借用由方法作用域保证 Rule 18）；Rule 14（session_editor 消费者 = App open / makeFrame 同切片交付；editor_new 保留 = EditorModel.init(buffer:) 测试便利并标注）；Rule 12（session.rs 316 → 151 + session/edit.rs 183、EditorModel 306 → 283 + IME 扩展、MetalView 316 → 251 + Viewport 扩展——T-073 遗留超限由本切片处置（T-018/T-045 同款拆分前置）） | Pass | T-073 遗留 MetalView 316 行超限（拆分前置于本切片，T-018 先例） | 2026-08-03 |
| T-063 + T-064 编辑热路径基准与行索引缓存（ADR-006 v1.2） | fa769cb | 行为证据：基线先行（Rule 16）——cargo bench 同配置（1s / 样本 10）实测：中间 insert 77.6ms / delete 76.5ms / 移动 309ms（改造前）→ 缓存落地后移动 378µs（−99.88%，criterion change 报告）；Core 全量 16 二进制绿（+1 缓存不变量单测：移动构建 / 编辑失效 / undo 失效）；fmt / clippy 零告警；Rule 9 三问（1 个 Option<Layout> 字段 + 4 处失效赋值，0 抽象层 / 0 依赖；复用既有 Layout 不可变快照，不违反 ADR-006）；Rule 16（前后量化对比）；Rule 12（editor.rs 303 → 283 + editor/tests.rs 24，拆分前置） | Pass | 无 | 2026-08-03 |
| T-023 性能基准体系 | aa78b6a | Rule 16 基准门禁 / Rule 3 文件 ≤300 / ADR-021 / Rule 9 三问（1 dev-dep、0 公共接口） | Pass | 无 | 2026-08-02 |
| T-032 测试与审计加固 | d867ac7 | Rule 8（proptest ADR-022）/ Rule 9 三问 / 审计留痕制度落地 | Pass（自审） | 无 | 2026-08-02 |
| T-018 水平滚动 | 47c1dc2 | ADR-019（0 公共 API、Core 不变）/ Rule 3（MetalView 298、TextRenderer 292 贴线，拆分后 258 / 130+200）/ Rule 9 三问（1 个视口状态 + 输入平移 + 光标可见性，无抽象层 / 无依赖）/ Rule 12（全部 ≤300，App 总量 1483 < 5,000；T-036 校正：现 1615）/ Rule 14（Viewport / VertexBuilder 均 App 模块内，非公共 API） | Pass | 无；拆分前置在本切片完成 | 2026-08-02 |
| T-036 存量清理 | 6ceb0fe | Rule 14（`Selection::clamp` 零消费者 → 撤销，ADR-007 v1.1 记录）/ Rule 12（core/src 1726 ≤20k；死代码移除）/ Rule 15（experience / audits 计数校正；T-032 hash 回填）/ I-006 核验纠正（.DS_Store 从未入库，误报撤销）；Rule 9 三问（0 抽象层 / 0 依赖，纯删除 + 文档校正） | Pass | I-006 误报：审查依据 find 输出未区分跟踪状态，已用 git ls-files 核验纠正 | 2026-08-02 |
| BUG-006 光标边缘留白 | 98cfb30 | ADR-019 光标横向可见性语义落实 / Rule 9（2 个留白常量 + 2 条边界规则，无新增依赖 / 抽象）/ Rule 12（Viewport 75、TextRenderer 132 等全部 ≤300） | Pass | 无 | 2026-08-02 |
| BUG-007 组合期间横向可见性 | 1d7dfe7 | ADR-019（组合末尾光标 = BUG-004 语义纳入可见性）/ Rule 9（1 行调用，无新增状态 / 依赖）/ Rule 12（MetalView+Input 99 行 ≤300） | Pass | 无 | 2026-08-02 |
| T-015 文件打开接线 | e6a7ebd | ADR-001 v1.1（Bridge FFI 3 项已记录）/ Rule 3（全部 ≤300）/ Rule 9 三问（3 个 FFI 适配函数 + App 薄胶水，0 抽象层 / 0 依赖）/ Rule 12（pub(crate) 访问器，不构成公共 API）/ Rule 14（DocumentManager 首次进产品，真实调用方 = AppDelegate） | Pass | 无；注册表副本与会话分离的激活状态边界已记录于 ADR-001 v1.1，随 T-024 统一 | 2026-08-02 |
| T-033 fuzz + 基准回归告警 | 9a60b08 | ADR-021 v1.1（反转「CI 不跑」经用户确认）/ ADR-022 v1.1（fuzz = 属性空间扩展，0 新依赖）/ Rule 7（对比脚本仅标准库）/ Rule 9（1 脚本 + 1 CI 作业 + 基线文件，无抽象层）/ Rule 12（bench-baseline 数据非源码，不计行数） | Pass | 无；criterion 自带 baseline 对比不因回归失败退出，自建脚本补齐（已记录 ADR-021 v1.1 原因） | 2026-08-02 |

## 规则

- 每切片必须新增一行；违规项必须写明处置（修代码 / ADR 修订 / 用户确认），
  不允许只写"通过"不写依据。
- **行为证据必填（T-039，I-007）**：审计项必须包含实际验证过的行为——运行的
  测试 / 基准命令、验证的边界或不变量、检查的门禁；只数行数 / API 数的审计
  不算审计。
- Commit 列填切片提交 hash；提交前无法预先填写的填「本切片」，由 git log 追溯。
- **回填机械门禁（T-039，I-007）**：CI-Docs 检查审计表——「本切片」只允许
  当前切片一行（未回填不得累积），表内 commit 必须存在于 git 历史；回填是
  合入前提，不再靠人工自觉。
- Roadmap 复审政策（季度 / 里程碑）触发全仓审计行，结论同步到 changelog。
